제 의견은 “구매 가능한 외부 종목을 가장 먼저 찾고, 그중 하나를 1분봉으로 거래”하는 구조는 가능합니다. 다만 무조건 매수는 모든 안전장치를 무시하는 의미가 아니라, 분석 조건은 순위 점수로 바꾸되 실행 안전조건은 반드시 유지하는 방식이어야 합니다.
1. tech_buy, alpha, 모델 선택을 차단 조건에서 순위 조건으로 변경
현재는 하나라도 통과하지 못하면 매수하지 않습니다. 단타 전용 모드에서는 다음 세 조건을 차단 조건이 아닌 감점 요소로 바꿀 수 있습니다.
tech_buy=False → 기술점수 감점
alpha 부족 → 종합점수 감점
모델이 선택하지 않음 → 모델점수 감점
gate=False → 의미·신뢰도 감점
그런 다음 구매 가능한 후보 중 종합점수가 가장 높은 한 종목을 선택합니다.
최종점수 =
  1분봉 모멘텀
  + 거래량 증가
  + VWAP 위치
  + 단기 추세
  + 모델 평가
  + 뉴스 점수
  - 스프레드
  - 변동성 위험
  - 체결비용
  - gate/alpha/기술조건 미충족 감점
하지만 다음은 절대로 우회하면 안 되는 하드 게이트입니다.
실제 잔액 부족
토스에서 심볼을 찾지 못함
거래정지·주문 불가
장 마감
현재가·호가가 오래됨
스프레드·예상 슬리피지가 너무 큼
미체결·상태 불명 주문이 이미 존재
일일 최대 손실 도달
중복 주문
손절 주문 실패
데이터/API 장애
즉 “구매 가능한 정상 후보가 한 개 이상 있으면 가장 높은 종목을 매수”는 가능하지만, 정상 후보가 0개인 상황에서도 매수하게 만들면 안 됩니다. 현금 보유가 최선인 시장 상태도 있기 때문입니다. 특히 단타는 적은 자본에서 거래비용과 시스템 장애의 영향이 커질 수 있다고 FINRA도 경고합니다. FINRA 단타 위험 고지
2. 현재 시스템은 아직 1분봉 단타 시스템이 아닙니다
대시보드에서 1분봉을 보여주는 것과 전략이 1분봉으로 매매하는 것은 별개입니다.
현재 자동매매 계산은 [main.py (line 777)](/C:/Users/hyunj/toss/main.py:777)에서 전략용 OHLCV를 요청하고, 실제 저장소는 interval="1d" 일봉을 사용합니다. 따라서 현재 tech_buy, trend, alpha, sdeP는 단타용 1분봉 판단이 아닙니다.
단타를 하려면 실행 흐름을 두 개로 분리하는 것이 좋습니다.
[하루 1회 또는 장 시작 전]
현재 KRW·USD 확인
→ Gemini가 저장 목록 밖까지 후보 10개 발견
→ 토스가 심볼·시장·현재가·거래 가능 여부 검증
→ 구매 가능한 최종 5개 확정
→ Super 분석
→ Ultra 매매·Risk 판단
→ GLM 최종 비교
→ 오늘의 단타 감시 종목 1~3개 확정

[장중 매분]
새 1분봉 마감 확인
→ 1분봉·5분봉·15분봉 특징 계산
→ 후보별 단타 점수 계산
→ 가장 높은 종목 선택
→ 하드 안전조건 통과 시 매수
→ 매수 후 매분 손절·익절·추세·보유시간 검사
→ 매도
→ 거래 보상과 리뷰 기록
모델 19회 호출을 1분마다 반복하면 너무 느리고 비쌉니다. 모델은 장 시작 전 종목과 전략을 정하고, 장중 1분 판단은 Python 계산으로 해야 합니다.
시장가 주문은 화면에서 본 가격과 다른 가격으로 체결될 수 있고 빠른 시장일수록 차이가 커질 수 있으므로, 단타에서는 주문 직전 호가와 슬리피지 검사가 특히 중요합니다. Investor.gov 주문 체결 설명
3. 에이전트 보상은 ‘강화학습’과 다릅니다
NVIDIA 모델에 거래 결과를 주고 평가하게 하는 것은 좋은 생각이지만, 그것만으로는 실제 강화학습이 아닙니다.
실제 강화학습은 다음이 있어야 합니다.
상태: 진입 당시 가격·거래량·VWAP·변동성·뉴스·포지션
행동: 매수·대기·매도·주문 비율
보상: 거래 결과
정책 갱신: 보상을 이용해 다음 행동 규칙이나 모델 파라미터 변경
모델이 “이번 거래는 잘했다/잘못했다”고 설명만 하면 이는 LLM 거래 리뷰어 또는 Critic입니다.
현재 구조에는 다음 방식이 현실적입니다.
Python이 숫자 보상 계산
→ NVIDIA Super가 거래 원인과 실수를 정성 리뷰
→ 결과를 trade_episode 로그와 agent memory에 저장
→ 다음 종목 질문에 최근 유사 거래 리뷰를 첨부
→ 전략별 최근 보상이 나쁜 경우 비중 축소
예시 보상은 다음과 같이 만들 수 있습니다.
reward =
  비용 차감 후 실현수익률
  - 최대 역행폭 패널티
  - 포트폴리오 낙폭 패널티
  - 손절 지연 패널티
  - 규칙 위반 패널티
매수·매도 체결가, 수수료, 슬리피지는 브로커 데이터로 Python이 계산하고, 모델이 숫자를 임의로 정하면 안 됩니다.
이 역할은 세 NVIDIA 모델 중 Super가 적합합니다.
Super: 체결 후 거래 리뷰·패턴 분류
Ultra: 다음 거래의 위험·진입·청산 계획
GLM: 여러 판단의 최종 비교
향후 실제 학습까지 하려면 별도의 PPO/DQN 같은 정책 모델과 학습·검증 데이터가 필요합니다. FinRL도 거래비용·유동성·위험회피를 포함한 시뮬레이션과 백테스트를 전제로 구성합니다. FinRL 원 논문
따라서 처음부터 실계좌에서 학습시키는 것이 아니라 다음 순서가 맞습니다.
과거 1분봉 재생 테스트
→ DRY RUN 실시간 모의매매
→ Paper 거래 보상 누적
→ 전략 고정 후 매우 작은 금액 LIVE
→ 이상 시 자동 중지
알고리즘 전략은 배포 전 테스트, 변경 후 검증, 실행 중 감시와 중지 장치가 필요하다는 것이 공식적인 위험관리 방향이기도 합니다. FINRA 알고리즘 거래 통제 지침
4. 실제 주문 후보 질문을 가장 먼저 실행하는 방법
지금도 기존 목록 기반 cash_affordable_candidates는 질문 CSV의 첫 번째입니다. 하지만 이것은 465개 안에서만 찾습니다.
새 구조에서는 CSV 순서만 바꾸기보다 실행 단계를 분리하는 게 낫습니다.
1순위: cash_symbol_discovery
       Gemini가 목록 밖 후보 발견

2순위: broker_candidate_validation
       토스에서 최종 5개 검증

3순위: intraday_candidate_analysis
       Super → Ultra → GLM

4순위: 1분봉 감시 및 실제 주문

5순위: 기존 우주·방산·로봇·국가·양자 등 장기 연구 질문
단타 엔진이 기존 3개월 테마 질문을 기다리면 안 됩니다. 느린 테마 질문은 주문 후보가 정해진 후 실행하거나 별도 백그라운드 작업으로 돌리는 편이 맞습니다.
600초 sleep도 분리해야 합니다.
종목 발견: 하루 한 번
테마 연구: 하루 한 번 또는 600초 주기
1분봉 단타: 다음 1분봉 마감 시각까지 대기
포지션 보유 중: 매분 또는 더 짧게 호가·청산 감시
단순히 LOOP_SECONDS=60으로 바꾸면 모델 호출 시간이 더해져 주기가 계속 밀립니다. 단타 엔진은 시계의 다음 분 경계에 맞춰 독립적으로 실행해야 합니다.
5. NoneType object is not iterable의 정확한 원인
이 부분은 코드와 실제 캐시에서 확인했습니다.
kr_domestic_top_candidates의 모델 실행은 최초 statistics_analyst가 insufficient_data를 반환했습니다. 그 결과 뒤의 Fact·뉴스·기술·Bull/Bear·Trader·Risk·최종 비교 역할이 전부 required_dependency_unavailable로 차단됐습니다.
그 실패 결과가 [state.json (line 91)](/C:/Users/hyunj/toss/state.json:91)에 다음처럼 저장됐습니다.
"selected_symbols": null,
"top_candidate_symbols": null
이후 당일 캐시를 읽은 [main.py (line 1727)](/C:/Users/hyunj/toss/main.py:1727)가 selected_symbols를 배열이라고 생각하고 반복합니다.
for symbol in llm_result.get("selected_symbols", [])
키가 없으면 []가 나오지만, 현재는 키가 있으면서 값이 null이므로 Python에서는 None이 되어 오류가 발생합니다.
그 다음 최상위 루프가 오류를 잡고 [ERROR]를 출력한 뒤 [main.py (line 2509)](/C:/Users/hyunj/toss/main.py:2509)의 600초 대기로 들어갑니다. 600초 후 전체 주기를 다시 시작하므로 질문이 또 나오는 것처럼 보입니다.
즉 모델이 정상적으로 다음 질문으로 넘어간 것이 아니라:
국내 이벤트 캐시 읽음
→ null 배열 반복
→ run_once 전체 중단
→ ERROR 출력
→ 600초 대기
→ 전체 주기 재시작
되는 버그입니다.
수정 방향은 명확합니다.
selected_symbols를 (값 or [])로 정규화
top_candidate_symbols, avoid_symbols도 같은 방식으로 정규화
실패 캐시는 null 대신 빈 배열 저장
한 이벤트 오류가 전체 주기를 죽이지 않도록 이벤트별 예외 격리
차단된 모델 결과는 오류가 아니라 안전한 HOLD로 처리
6. 확률미적분학.txt
지정하신 C:\Users\hyunj\Downloads\확률미적분학.txt 파일은 현재 해당 경로에 존재하지 않았고, Downloads의 다른 .txt 파일에서도 찾지 못했습니다. 그래서 문서의 구체적인 식을 실제로 읽고 평가하지는 못했습니다.
파일을 다시 그 경로에 저장하거나 프로젝트 안에 넣으면 다음 항목으로 연결해서 검토할 수 있습니다.
일정 시간 안에 익절선에 먼저 도달할 확률
손절선보다 익절선에 먼저 도달할 확률
1분봉 변동성 추정
예상 보유시간
비용 차감 기대값
현재 sdeP의 단타용 재설계
거래 보상 함수의 위험 패널티
최종적으로 추천하는 방향은 **“하드 안전조건을 통과한 후보 중 반드시 1등을 선택하는 강제선택 모드 + 목록 밖 Gemini 종목 발견 + NVIDIA 3모델 검토 + 독립된 1분봉 실행 엔진 + Python 숫자 보상과 Super 사후 리뷰”**입니다. 모든 위험조건까지 없애는 무조건 매수 방식은 권하지 않습니다.