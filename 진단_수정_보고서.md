# 자동매매 미작동 진단·수정 보고서 (2026-07-24)

## 1. 이 프로젝트가 뭔가

토스증권 Open API로 국내·해외 주식을 자동매매하는 LLM 기반 퀀트 봇.
구조는 ai_prompot 폴더의 논문 11편을 그대로 반영한 설계다.

- 02 트레이딩팀(TradingAgents): 분석가·연구원·트레이더·리스크 역할 분리 → `ai_prompts/agents`, `strategy/agentic/orchestrator.py`
- 03 사실주관(FS-ReasoningAgent): 사실/주관 분리 추론 → orchestrator의 fact_reasoner / subjectivity_reasoner
- 04 재현감사(Execution Assumptions): 실데이터 강제, 체결 전 재확인, 주문 원장 → `live_data_guard.py`, `order_lifecycle.py`, LIVE BLOCKED 계열 가드
- 05 매매시뮬(Can LLMs Trade?): 호가창·시장가 검증 → `validate_market_order_cost`, orderbook 체크
- 08 이중증류: Teacher(LLM)/Student(규칙) 분리 → LLM은 승인만, 주문은 결정론적 규칙 → `distillation.py`
- 09 에이전트기억(A-MEM): `memory_store.py`
- 10 위험필터(LLM as a Risk Manager): 통계 후보를 LLM이 의미론적으로 걸러 축소 → `lead_lag.py` + semantic risk gate
- 11 추측디코딩: `speculative_decoding.py` (성능 최적화, 매매와 무관)

## 2. 왜 지금까지 단 한 번도 매수가 없었나 (로그 4,802건 전수 분석)

trades.jsonl 8일치 결과: **BUY 0건, HOLD 2,268건, SELL_SIGNAL_NO_POSITION 2,449건, 에러 85건.**
원인은 하나가 아니라 4겹으로 쌓여 있었고, 각 겹이 단독으로도 매수를 100% 차단했다.

### 겹 1 — 브로커 연결이 밤새 죽음
- 7/8: 토스 토큰 발급 403 `IP address not allowed` (IP 미등록 시기)
- 7/15 23:46 ~ 7/16 11:05: `cannot parse buying power` **69회 연속** → 이 예외가 run_once 전체를 중단시켜 그날 루프가 전부 죽음
- 7/16 12:30~14:00: OHLCV 조회 429 rate-limit (이벤트 6개 × 종목 최대 200개 = 과도한 호출)
- 그 결과 `state.json`의 `api_error_count=85` ≥ 상한 5 → survival gate가 "too many api errors"로 매수 영구 차단

### 겹 2 — LLM 신뢰도가 구조적으로 0.55를 넘을 수 없음
llm_calls.jsonl 전수 확인:
- **NVIDIA NIM: 실호출 30회 전부 404 "page not found" 실패** → 매번 mock fallback confidence=0.40
- Google Gemini: 400 에러 20회("Tool use with response mime type json unsupported" = grounding+JSON 동시 요청 충돌), 성공 6회
- 실측 confidence 분포: **0.0(1,494건)과 0.4(3,223건)뿐** → MIN_LLM_CONFIDENCE=0.55를 단 한 번도 통과 못 함 → BUY 불가

### 겹 3 — event_edge가 설계상 0으로 붕괴
`event_edge = llm_prob − LMSR 내부가격`인데 매 루프 `alpha=0.40`으로 내부가격을 LLM 확률 쪽으로 당김 → 몇 루프 만에 edge≈0 → `MIN_EVENT_EDGE=0.08` 영구 미달. 게다가 edge≈0이면 `final_alpha ≤ 0.10` SELL 조건에 걸려 **전 종목이 SELL로 분류**(SELL_SIGNAL_NO_POSITION 2,449건의 정체).

### 겹 4 — AND 게이트 10개 직렬
BUY가 나가려면 edge≥0.08 ∧ final_alpha≥0.55 ∧ sde≥0.58 ∧ trend≥0.2 ∧ 기술적 진입확인 ∧ confidence≥0.55 ∧ 에이전트 승인 ∧ 에이전트 종목선택 ∧ 예산/한도 ∧ 호가검증. 각각은 논문(04, 10)의 취지에 맞지만, 입력이 mock(가격 100.0, confidence 0.4)인 상태에서는 통과 확률이 수학적으로 0이었다.

## 3. 수정 내역 (오늘 적용)

| 파일 | 수정 | 근거 논문 |
|---|---|---|
| `main.py` | buying-power 파싱 실패 시 루프 전체 중단 → 해당 통화만 0원 처리하고 루프 계속(포지션 감시·exit는 유지) | 04: 실행 견고성, fail-closed 유지 |
| `strategy/live_data_guard.py` | buying-power 응답 키 변형(list/이름 변형) 대응 파서 보강 | 04 |
| `strategy/llm_pipeline.py` | NVIDIA 실패/예산초과 시, **실제 Google evidence가 있으면** 그것으로 판단 구성(confidence×0.85 페널티). Google도 mock이면 기존 0.40 fallback 유지 → 여전히 fail-closed | 02·03: 단일 제공자 장애가 전체 판단을 무작위 mock으로 강등시키지 않게 |
| `strategy/risk.py` | SELL 임계값 하드코딩(0.10/-0.30/0.45) → .env의 SELL_IF_* 설정 연결 | 04: 파라미터 명시·재현성 |
| `main.py`+`config.py` | LMSR 수렴속도 0.40 하드코딩 → `LMSR_UPDATE_ALPHA=0.15` 설정화 | 겹 3 해소 |
| `.env` | MAX_SYMBOLS_PER_EVENT 200→40(429 방지), MIN_EVENT_EDGE 0.08→0.05, MIN_FINAL_ALPHA 0.55→0.35, MIN_SDE_PROB_UP 0.58→0.55, GOOGLE_DAILY_CALL_LIMIT 10→50 | 도달 가능하되 보수적인 게이트 |
| `state.json` | api_error_count 85→0 리셋 | 겹 1 잔재 제거 |

검증: `py_compile` 통과, 단위 로직 테스트(파서·판단·decide_action) 통과, **기존 테스트 49/49 통과.**

## 4. 남은 사용자 조치 (코드로 해결 불가)

1. **NVIDIA 404**: `NVIDIA_MODEL=deepseek-ai/deepseek-r1`이 현재 카탈로그에 있는지 build.nvidia.com에서 확인 후 .env 갱신. (실패해도 이제 Google 기반 판단으로 대체됨)
2. **pykrx 미설치**: 실행 환경에서 `pip install -r requirements.txt` (로그에 "pykrx is not installed" 반복)
3. **토스 IP 등록**: 다른 네트워크에서 실행하면 403 재발. 고정 IP 또는 실행 위치 고정.
4. 재가동 순서: `python main.py --preflight`로 브로커 연결 확인 → `DRY_RUN=true`로 1~2일 관찰(BUY 신호가 실제로 생성되는지) → 이상 없으면 `DRY_RUN=false`.

## 5. 수익률에 대해 솔직하게

수익률은 어떤 시스템도 보장할 수 없다. 이번 수정은 "매수가 아예 불가능했던 결함"을 제거한 것이지 알파를 보장하는 게 아니다. 04 논문(재현감사)의 결론 그대로: 성과는 데이터 시점·체결 가정·비용에 좌우되므로, 라이브 투입 전 DRY_RUN 기간의 신호·가상체결 기록으로 기대 수익과 비용(수수료+슬리피지)을 먼저 검증할 것. 소액(현 설정: 회당 15,000원, 총 30,000원 캡)으로 시작하는 현재 한도는 유지 권장.
